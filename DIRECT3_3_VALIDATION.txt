ORYN Direct ESP32 V10.4.1 — Direct3.3 Core Fix Validation

Reported real-device failures addressed:
- Browse Patterns: "Failed to load patterns" / 0 patterns after ESP32 activation.
- Machine state did not promote to connected after Smart Wi-Fi + FluidNC detection.
- Home did not execute through a reliable Direct motion lifecycle.

Architecture correction:
- Direct table now keeps the ORYN frontend on the app.oryn origin.
- Pattern/library requests remain on the proven Mobile offline API shim.
- Only machine-control endpoints are routed to the native Direct FluidNC bridge.
- Native status is surfaced through the existing app.oryn status WebSocket shim.

Automated static/runtime checks performed in build workspace:
- offline .thr files present: 100
- offline catalog entries: 100
- catalog referenced THR/preview files: present
- Pattern Designer app.js syntax: PASS
- Pattern Designer mobile integration JS syntax: PASS
- ORYN mobile bootstrap JS syntax: PASS
- Node mocked-runtime Direct bridge test:
    pattern list -> HTTP 200, 100 patterns
    /connect -> HTTP 200, FluidNC probe success
    /send_home -> HTTP 200, asynchronous home accepted
    /api/table-info -> HTTP 200, Direct table identity
- Java source javac parse pass reached Android unresolved-symbol stage with no Java syntax/parser errors found.

Motion preservation:
X = theta / (2*pi) * saved theta revolution units
Y = rho * saved rho travel units * rho direction
Formula unchanged from Direct3.2.

Current small-table values already verified from prior ORYN logs:
Theta full revolution = 78.00000 controller units
Rho center -> perimeter = 51.95800 controller units
Rho direction = +1
Physical radius = 110 mm (informational only; do not enter as controller units)
Initial Direct feed = 60 for first safe test

ESP32 note:
Do not reflash the controller just to install this Android update. Current ESP32 manual X/Y motion was already confirmed working.
