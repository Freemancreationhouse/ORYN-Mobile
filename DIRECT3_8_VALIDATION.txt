ORYN Direct ESP32 V10.4.1 — DIRECT3.8 Live Calibration Validation

STATIC CHECKS
- oryn-mobile-bootstrap.js syntax: PASS (node --check)
- oryn-v33-exact-ui.js syntax: PASS (node --check)
- Direct calibration POST routing: PASS
- Direct Full-Circle GET/start/jog/set mocked runtime: PASS
- Direct Perimeter start/jog/save mocked runtime: PASS
- Saved Direct calibration immediately updates oryn_direct_last: PASS
- Pattern/Home motion lock while calibration active: implemented

RUNTIME MOCK RESULTS
- GET /api/rotation-calibration -> HTTP 200
- POST /api/rotation-calibration/start -> HTTP 200, G92 X0
- POST /api/rotation-calibration/jog (1 unit) -> HTTP 200, G91 / G1 X1 / G90
- POST /api/rotation-calibration/set (79.25) -> HTTP 200, saved thetaRev=79.25
- POST /api/perimeter-calibration/start -> HTTP 200, G92 Y0
- POST /api/perimeter-calibration/jog (5 units) -> HTTP 200, G91 / G1 Y5 / G90
- POST /api/perimeter-calibration/save -> HTTP 200, saved rhoTravel=5 in mock

PRESERVATION
- Existing native Direct pattern engine unchanged.
- Existing Direct Home implementation unchanged.
- Theta-Rho conversion formula unchanged.
- Smart Wi-Fi / hotspot support unchanged.
- Offline library, Pattern Designer and Pattern Forge unchanged.
- MainActivity.java changed only in app version string.

NOTE
Calibration requires ORYN Direct — ESP32 FluidNC to be the active selected table. ORYN Offline intentionally denies physical machine motion.
