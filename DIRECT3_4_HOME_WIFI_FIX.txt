ORYN Direct ESP32 V10.4.1 DIRECT3.4 HOME-WIFI FIX

Fixes:
- Home-WiFi/STA motion sockets use Android default network instead of stale local-only FluidNC AP binding.
- FluidNC LAN discovery sends a real newline to Telnet ($I), fixing false “No FluidNC controller found”.
- Direct ESP32 active-table selection survives app cold restart.
- Home is idempotent if auto-home is already running.
- Home endpoint reports native bridge errors instead of a generic false failure.
- Saved Direct host is prefilled in Manual IP fallback.

No Theta-Rho conversion mathematics, calibration numbers, Pattern Designer, Pattern Forge or pattern files changed.
